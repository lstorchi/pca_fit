#include "pcaconst.hpp"

// questo e' il rapporto fra scaling factor di phi ed r
const long long int rpdval = 64;

int check_val(double val, int width, std::string mstr); 
int get_missing_layer(pca::matrixpcaconst<double> c);
